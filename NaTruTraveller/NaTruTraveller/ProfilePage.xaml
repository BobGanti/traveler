<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:NaTruTraveller.ViewModels"
             x:Class="NaTruTraveller.ProfilePage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:ProfileVM x:Key="vm"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <ContentPage.Content>
        <StackLayout Margin="15,40,15,0"
                     BindingContext="{StaticResource vm}">
            <Label x:Name="postCountLabel"
                   HorizontalOptions="Center"
                   Text="{Binding PostCount}"
                   TextColor="DodgerBlue"
                   FontAttributes="Bold"/>
            <Label Text="Orders"
                   HorizontalOptions="Center"
                   TextColor="Gray"/>
            <ListView x:Name="venueListView"
                      ItemsSource="{Binding Categories}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell x:Name="catNameSource" Text="{Binding Path=VenueName}"
                            Detail="{Binding Path=Count}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>